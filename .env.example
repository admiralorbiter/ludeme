# Ludeme — Environment Variables
#
# Copy this file to .env and fill in your values.
# Never commit .env to version control.

# SQLite database path (relative to project root, or use absolute path)
DATABASE_URL=sqlite:ludeme.db

# Port for the Axum backend server
SERVER_PORT=3000
